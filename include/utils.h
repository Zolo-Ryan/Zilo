#include<main.h>
#include<zBuffer.h>

#ifndef UTILS_H
#define UTILS_H
    int is_seperator(int);
    int is_open_character(int);
    int digits(int);
    int allClean(zBuffer);
    int charptrLen(char*);
    char *charptrName(char*);
#endif